from django.contrib import admin
from photo2.models import nature,bird,reptile,insects,travels
# Register your models here.
admin. site. register(nature),
admin. site. register(bird),
admin. site. register(reptile),
admin. site. register(insects),
admin. site. register(travels),

